#!/bin/bash
# Script based on Digital Ocean OpenVPN tutorial
# First argument: Client identifier

KEY_DIR=/home/{{ admin_user }}/openvpn-ca/keys
OUTPUT_DIR=/home/{{ admin_user }}/client-configs/files
BASE_CONFIG=/home/{{ admin_user }}/client-configs/base.conf

cat ${BASE_CONFIG} \
    <(echo -e '<ca>') \
    ${KEY_DIR}/ca.crt \
    <(echo -e '</ca>\n<cert>') \
    ${KEY_DIR}/${1}.crt \
    <(echo -e '</cert>\n<key>') \
    ${KEY_DIR}/${1}.key \
    <(echo -e '</key>\n<tls-auth>') \
    ${KEY_DIR}/ta.key \
    <(echo -e '</tls-auth>') \
    > ${OUTPUT_DIR}/${1}.ovpn